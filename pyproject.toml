[project]
name = "md-editor-models"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Antigravity Bot", email = "bot@antigravity.local" }
]
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5.0.1",
    "pangu>=5.0.0",
    "peft>=0.20.0",
    "torch>=2.13.0",
    "transformers>=5.16.1",
    "trl>=1.12.0",
]

[project.scripts]
md-editor-models = "md_editor_models:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
